import { StepperButtonDirective } from './stepper-button.directive';

describe('StepperButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperButtonDirective();
    expect(directive).toBeTruthy();
  });
});
